<!DOCTYPE html>
<html>
  <head>
    <title>PetitCachottier</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/kute.js/2.0.0/kute.min.js"></script>
  </head>

  <body>
    <nav class="navbar navbar-expand-md" id='nav1'>
      <div class="container-fluid">
        <%= link_to 'Petit Cachottier', root_path, class: "navbar-brand text-white", data: { turbolinks: false } %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav">
            <li class="nav-item active">
              <%= link_to "L'équipe", team_path, class: 'nav-link text-violet-blue-crayola', data: { turbolinks: false } %>
            </li>
            <li class="nav-item active">
              <%= link_to "Contact", contact_path, class: 'nav-link text-violet-blue-crayola', data: { turbolinks: false } %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <%= yield %>
  </body>
</html>
